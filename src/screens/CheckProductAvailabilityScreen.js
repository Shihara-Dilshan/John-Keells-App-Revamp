import React from 'react';
import {View, Text} from 'react-native';

export default function CheckProductAvailabilityScreen({navigation}) {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Check product availability Screen</Text>
    </View>
  );
}
